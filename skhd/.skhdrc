########### ~/.skhdrc ############
#
#  CAPS LOCK AS HYPER + VIM-LIKE YABAI CONTROLS
#  ────────────────────────────────────────────
#  Hold ⇪ (Caps Lock) and press h-j-k-l to move focus
#  Hold ⇪ + Shift to swap (move) windows
#  Hold ⇪ + Ctrl to resize
#  Hold ⇪ + Option to jump desktops (Spaces)
#  Hold ⇪ + Option + Shift to send windows to Spaces
#  Hold ⇪ + Cmd to work with Displays
#
###################################

# ── Basic window focus (Vim movement) ───────────────────────────────
caps_lock - h  : yabai -m window --focus west
caps_lock - j  : yabai -m window --focus south
caps_lock - k  : yabai -m window --focus north
caps_lock - l  : yabai -m window --focus east

# ── Swap / move windows (add Shift) ────────────────────────────────
shift + caps_lock - h : yabai -m window --swap west
shift + caps_lock - j : yabai -m window --swap south
shift + caps_lock - k : yabai -m window --swap north
shift + caps_lock - l : yabai -m window --swap east

# ── Resize windows (add Ctrl) ──────────────────────────────────────
ctrl + caps_lock - h : yabai -m window --resize left:-20:0
ctrl + caps_lock - j : yabai -m window --resize bottom:0:20
ctrl + caps_lock - k : yabai -m window --resize top:0:-20
ctrl + caps_lock - l : yabai -m window --resize right:20:0

# ── Balance / float / fullscreen toggles ───────────────────────────
caps_lock - space   : yabai -m space  --balance          # re-even gaps
caps_lock - return  : yabai -m window --toggle float     # toggle float
caps_lock - f       : yabai -m window --toggle zoom-full # fullscreen

# ── Desktop (Space) navigation: ⇪ + ⌥ + number ────────────────────
alt + caps_lock - 1 : yabai -m space --focus 1
alt + caps_lock - 2 : yabai -m space --focus 2
alt + caps_lock - 3 : yabai -m space --focus 3
alt + caps_lock - 4 : yabai -m space --focus 4
alt + caps_lock - 5 : yabai -m space --focus 5

# ── Send window to Space: ⇪ + ⌥ + ⇧ + number ─────────────────────
shift + alt + caps_lock - 1 : yabai -m window --space 1 && yabai -m space --focus 1
shift + alt + caps_lock - 2 : yabai -m window --space 2 && yabai -m space --focus 2
shift + alt + caps_lock - 3 : yabai -m window --space 3 && yabai -m space --focus 3
shift + alt + caps_lock - 4 : yabai -m window --space 4 && yabai -m space --focus 4
shift + alt + caps_lock - 5 : yabai -m window --space 5 && yabai -m space --focus 5

# ── Display (monitor) navigation: ⇪ + ⌘ + n / p ──────────────────
cmd + caps_lock - n : yabai -m display --focus next
cmd + caps_lock - p : yabai -m display --focus prev

# ── Send window to other display: ⇪ + ⌘ + ⇧ + n / p ──────────────
shift + cmd + caps_lock - n : yabai -m window --display next
shift + cmd + caps_lock - p : yabai -m window --display prev
###################################
